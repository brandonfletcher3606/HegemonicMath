#include "Matrix.h"

// template<class T, std::size_t ROWS, std::size_t COLUMNS>
// bool Hegemonic::Matrix<T, ROWS, COLUMNS>::isSquare() const
// {
//     return mMatrix.size() == mMatrix[0].size();
// }